package dev.archunitjava.rules;

/** Deterministic operations over explicitly encoded, typed annotation elements. */
public enum AnnotationValueOperator {
    PRESENT,
    ABSENT,
    EQUALS,
    NOT_EQUALS
}
