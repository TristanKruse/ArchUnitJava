package dev.archunitjava.projection;

/** Target identity domain for one deterministic graph projection. */
public enum ProjectionDomain {
    PACKAGE,
    TYPE,
    MEMBER,
    CLASSPATH,
    MODULE
}
