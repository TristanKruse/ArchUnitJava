package dev.archunitjava.model;

/** Machine-readable failures while adapting parsed class headers into Java types. */
public enum TypeModelDiagnosticCode {
    INVALID_BINARY_NAME,
    MODULE_DESCRIPTOR_IS_NOT_A_TYPE
}
